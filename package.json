{
  "name": "z-canvas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "zig build wasm && vite build",
    "build:zig": "zig build wasm",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.8.0",
    "vite": "^6.3.0"
  }
}
