{
  "name": "z-canvas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "zig build wasm && vite",
    "build": "zig build wasm && vite build",
    "build:zig": "zig build wasm",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.3.0"
  }
}
